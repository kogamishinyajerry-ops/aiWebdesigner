NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Cloud Studio 环境变量（自动检测）
# 如果需要在 Cloud Studio 中访问，前端需要使用 3000 端口，后端使用 8000 端口
# 在浏览器中通过 Cloud Studio 预览时，localhost 会指向 Cloud Studio 的代理
